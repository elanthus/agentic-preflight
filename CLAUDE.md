# Repository instructions

After creating a pull request, always print the exact Bash commands the user can run
to install or update Agentic Preflight from that pull request's branch and then refresh
the installed agent integrations. Use the actual repository and branch names; do not
leave placeholders for the user to fill in.
